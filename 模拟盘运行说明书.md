# 模拟盘运行说明书

> qlib 选股 → QMT 模拟盘自动调仓 的端到端操作手册
> 最后更新：2026-05-31

---

## 0. 一句话总览

每个交易日，**同一台 Windows 机**自动完成：上午用 qlib 模型算出当天该买哪几只股票并写成目标文件，下午把 QMT 模拟账户调仓到与目标一致。两步由 Windows 任务计划程序定时拉起，平时无需人工干预。

```
08:30  daily_signal.ps1   拉 Tushare 数据 → qlib 打分选股 → 写 target_今天.json
14:25  daily_trade.ps1    读 target_今天.json → 调仓下单到 QMT 模拟端
```

---

## 1. 环境与前置条件

| 项 | 要求 |
|---|---|
| 仓库位置 | `C:\Users\Administrator\Documents\stock\`（**含 `stock` 中间层，所有配置已按此设定**） |
| qlib 环境 | `stock\qlib_quant\.venv\Scripts\python.exe`（pyqlib，跑数据/选股） |
| QMT 环境 | `C:\Python312\python.exe`（装了 `xtquant`，跑下单） |
| Tushare Token | 写在 `stock\qlib_quant\.env` 的 `TUSHARE_TOKEN=...` |
| 行情/交易端 | **下午调仓前必须先启动并登录国金 QMT 模拟交易端（MiniQMT）** |

> 两个 Python 解释器分工不同，不要混用：选股用 `.venv`，下单用 `C:\Python312`。

---

## 2. 三个子项目

| 目录 | 角色 | 关键文件 |
|---|---|---|
| `qlib_quant/` | 研究 + 选股大脑 | `main.py`、`config/models/*.yaml`、`scripts/export_target.py` |
| `qmt/` | 实盘执行层 | `live_trade.py`、`rebalancer.py`、`live_config.py`、`scheduler/` |
| `qlib_ui/` | 回测可视化（可选，本流程不依赖） | `backend/`、`frontend/` |

---

## 3. 当前策略口径

- **模型**：`alpha158_momentum_volume_k6_dd10_overlay`（进攻型量价，CSI300 池，双周调仓，topk=6）
- **仓位**：固定 80%（留 20% 现金）
- **目标文件**：每只等权（6 只各约 16.67%）

> 注：导出脚本 v1 用固定 80% 仓，**未复刻 YAML 里的 overlay 动态风控**（target_vol / 回撤降仓），所以模拟盘实际仓位比回测更激进，属已知简化。
>
> README 标称的历史最优是「主备 60/40 组合」（alpha158 0.6 + push25_cq10_v3_vol_norm 0.4），如需切换见 §7。

---

## 4. 日常自动运行（已配置好，了解即可）

两个任务计划由 `qmt\scheduler\register_tasks.ps1` 注册（周一~五）：

| 任务名 | 时间 | 脚本 | 做什么 |
|---|---|---|---|
| `QlibDailySignal` | 08:30 | `daily_signal.ps1` | ① `main.py update` 更新数据 ② 校验数据/预检是否真通过 ③ `export_target.py` 写 `target_今天.json` |
| `QmtDailyTrade` | 14:25 | `daily_trade.ps1` | 检查 target 存在 → `live_trade.py --live` 调仓 |

**首次启用 / 重装任务计划**（管理员 PowerShell）：
```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\Administrator\Documents\stock\qmt\scheduler\register_tasks.ps1"
```

日志在 `qmt\scheduler\logs\`：`signal_YYYYMMDD.log/.err`、`trade_YYYYMMDD.log/.err`。
执行报告在 `qmt\reports\exec_YYYYMMDD.json`。

---

## 5. 手动跑一遍（排查 / 临时执行）

全部从对应子目录执行。

### 5.1 更新数据
```powershell
cd C:\Users\Administrator\Documents\stock\qlib_quant
# .env 里有 TUSHARE_TOKEN；若手动跑需先注入环境变量（见下方提示）
.\.venv\Scripts\python.exe -X utf8 main.py update
```
> 手动跑时 token 不会自动从 .env 进环境，需先注入。最稳妥是直接跑 `daily_signal.ps1`，它会自动读 .env 注入。

### 5.2 生成今天的目标文件
```powershell
cd C:\Users\Administrator\Documents\stock\qlib_quant
.\.venv\Scripts\python.exe -X utf8 scripts\export_target.py `
    --config config\models\alpha158_momentum_volume_k6_dd10_overlay.yaml `
    --target-dir C:\Users\Administrator\Documents\stock\qmt\targets
# 只看选股不写文件：加 --dry-run
```

### 5.3 调仓（先干跑，确认无误再实盘）
```powershell
cd C:\Users\Administrator\Documents\stock\qmt
# 先启动并登录 MiniQMT 模拟端！
C:\Python312\python.exe live_trade.py --dry-run   # 只算计划订单，不下单
C:\Python312\python.exe live_trade.py --live      # 真正下单到模拟账户
```

### 5.4 直接跑调度脚本（推荐的手动方式，自动注入 token）
```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\Administrator\Documents\stock\qmt\scheduler\daily_signal.ps1"
powershell -ExecutionPolicy Bypass -File "C:\Users\Administrator\Documents\stock\qmt\scheduler\daily_trade.ps1"
```

---

## 6. 关键配置文件

| 文件 | 改什么 |
|---|---|
| `qmt\live_config.py` | `DRY_RUN`（True=只算不下单）、`SLIPPAGE_BPS` 滑点、`MAX_ORDER_NOTIONAL` 单笔上限、`MAX_TURNOVER` 换手率上限、`TARGET_DIR`、kill-switch 文件路径 |
| `qmt\config.py` | QMT 连接参数（IP / 端口 / 账户 / 默认复权） |
| `qmt\scheduler\*.ps1` | 定时时间、用哪个策略 config |
| `qlib_quant\config\paths.yaml` | 数据路径（**移动目录后必须改**，见 §8） |
| `qlib_quant\config\models\*.yaml` | 模型/选股/仓位参数 |

### 紧急停止下单（kill-switch）
新建空文件即可让 `live_trade.py` 拒绝一切下单：
```
C:\Users\Administrator\Documents\stock\qmt\STOP
```
删掉该文件即恢复。

### 切回纯模拟（不真下单）
把 `qmt\live_config.py` 的 `DRY_RUN = True`，或调度里用 `--dry-run`。
> 注意：`daily_trade.ps1` 默认用 `--live`，模拟盘下它打到模拟账户；要彻底不下单走 `--dry-run` 或 kill-switch。

---

## 7. 切换策略

### 单策略
改 `qmt\scheduler\daily_signal.ps1` 第 16 行 `$CONFIG` 指向另一个 `config\models\*.yaml` 即可。

### 主备 60/40 组合（需要时）
`export_target.py` 当前为单策略版。要跑组合需先扩展该脚本支持按配比合并两个子策略的选股，再把 `daily_signal.ps1` 改成组合调用。这是一处改造，需要时再做。

---

## 8. 移动 / 重装目录后必须同步的路径（重要）

路径**散在三层**，挪动仓库后三层都要改，否则会静默走错目录甚至重建空壳：

1. **qmt 执行层**：`live_config.py`（TARGET_DIR / REPORT_DIR / KILL_SWITCH_FILE）、`scheduler\*.ps1`（QLIB_DIR / QMT_DIR / SCHED_DIR / TARGET_DIR / LOG_DIR）、`scripts\export_target.py` 默认 target-dir。
2. **qlib 配置层**：`config\paths.yaml`、`config\trading.yaml` 里的家目录路径。
3. **qlib 源码 fallback（最隐蔽）**：约 33 处 `CONFIG.get("paths.xxx", "~/Documents/...")` 的兜底默认值。**只要在 `paths.yaml` 顶层提供 `qlib_data` / `selections` / `cache` 键即可一处覆盖全部**，不必改源码。

> 另有一个独立坑：`modules\data\tushare_downloader.py` 的 `TushareDownloader()` 构造默认路径**不读配置**。补数据请走 `main.py update`，不要直接 new 这个类。

验证路径是否正确：
```powershell
cd C:\Users\Administrator\Documents\stock\qlib_quant
.\.venv\Scripts\python.exe -X utf8 -c "from config.config import config; print(config.get('paths.qlib_data'))"
# 输出应含 ...\stock\qlib_quant\... 且非 None
```

---

## 9. 故障排查

| 现象 | 原因 / 处理 |
|---|---|
| `daily_trade` 报 "target 文件不存在" | 上午 `daily_signal` 没成功；查 `signal_*.err`，多半是 token 或数据预检 |
| update 打 "Tushare API 不可用 / api init error" | token 没进环境。跑 `daily_signal.ps1`（自动注入），或确认 `.env` 有 `TUSHARE_TOKEN` |
| update 显示成功但选股没更新 | **软失败**：update 预检不过却返回 exit 0。`daily_signal.ps1` 已加输出校验拦截；手动判断看 `config.get('paths.qlib_data')` 目录下 `factor_data.parquet` 的修改时间 |
| 预检报 "index_weight 最早快照晚于回测起点" | 历史成分不足。`main.py update` 会自动补；仍不行手动补 2018-12 起的 index_weight |
| 下单全部 "无可卖量" | T+1 限制，当天买入的次日才可卖，正常 |
| 下单 "涨停无法买入 / 跌停无法卖出" | A 股涨跌停规则，正常跳过 |
| `live_trade` 连得上行情却下不了单 | QMT 交易端没登录。先登录 MiniQMT 模拟端 |
| 任意脚本卡住要急停 | 建 `qmt\STOP` 文件 |

---

## 10. A 股规则与安全设计（已内置，了解即可）

`rebalancer.py` 计划订单时已处理：
- **T+1**：只卖 `can_use_volume`（可用量），当天买入不计入
- **100 股整手**：买卖按整手，仅清仓时允许卖零股
- **涨跌停**：涨停不买、跌停不卖（用 QMT `UpStopPrice`/`DownStopPrice`）
- **停牌/无报价**：跳过
- **先卖后买**：先腾出现金再买入
- **现金约束**：买入受可用资金 + 当日卖出回款限制
- **风控闸**：单笔超 `MAX_ORDER_NOTIONAL` 缩量、换手超 `MAX_TURNOVER` 中止
- **幂等**：`live_trade.py` 是"调到目标"，重复运行只补差额；目标已达成则零下单

封装层（`qmt_data.py` / `qmt_trade.py`）出错返回 `None`/`[]` 并打 `[ERROR]`，不抛异常；执行层（`live_trade.py`）相反——预检不过就大声中止，绝不拿坏数据下单。
