# 策略开发计划

## 目标
基于最强因子组合 + 动态止损调仓的低换手策略

---

## 步骤

### 1. 选择最强因子组合
- 基于全周期(2019-2026)回测结果
- 当前最优单因子：
  - `book_to_price` — 年化5.8%，夏普0.41
  - `ocf_to_ev` — 年化5.5%，夏普0.39
- 组合方向：价值因子为主

### 2. 按照强因子组合选股
- 使用选定的因子组合计算综合得分
- 选出Top-K只股票作为持仓池
- 初始建仓时一次性买入

### 3. 动态调仓逻辑
**每日检查，条件触发才调仓：**

- **触发条件**：持仓股票从近期最高点下跌 >= 10%
- **调仓动作**：卖出该股票，从因子得分排名靠前的非持仓股票中选择一只买入
- **不调仓情况**：如果没有股票跌破10%阈值，则当日不进行任何操作

```
伪代码：
for each holding_stock:
    recent_high = max(holding_stock.price, lookback=20d)
    drawdown = (holding_stock.price - recent_high) / recent_high
    if drawdown <= -10%:
        sell(holding_stock)
        buy(best_available_stock_from_factor_pool)
```

---

## 参数待定
- [ ] 因子组合权重
- [ ] Top-K 选股数量
- [ ] 近期最高点回看天数 (默认20日)
- [ ] 止损阈值 (默认10%)
- [ ] 备选股票池大小

---

## 预期优势
1. **低换手**：只在止损触发时调仓，避免频繁交易的手续费侵蚀
2. **顺势持有**：让盈利的股票继续持有，截断亏损
3. **因子驱动**：换入的股票来自因子选股池，保证质量

---

## 下一步
- [ ] 实现动态调仓回测模块
- [ ] 测试不同止损阈值(8%/10%/12%/15%)
- [ ] 对比固定周期调仓 vs 动态调仓的收益差异
