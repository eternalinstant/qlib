
# 数据质量检查问题报告 - 2026-05-03 19:11:39

> **事后勘误 (2026-05-05)**：Tushare 源文件完整性检查为**误报**（false positive）。
> 所有 9 个 parquet 文件 (`daily_basic`, `adj_factor`, `income`, `balancesheet`, `cashflow`, `fina_indicator`, `index_daily`, `index_weight`, `namechange`) 在检查运行时均已存在于 `data/tushare/`。
>
> **根因**：`data_quality_guard.py` 主循环在 `--max-retries=1` 时，先执行 Phase 1 检查（获取报告 report），再执行 Phase 3 修复（下载/更新），循环结束后用修复**前**的 report 写入 Q&A。修复动作即使成功也未被重新验证。
>
> **已修复**：guard 主循环在退出前增加终检步骤（`final_report = self.run_full_check()`），Q&A 永远写入最新状态。
> 同时给 `validate_data.py` 增加了 tushare_dir 路径日志，方便调试类似问题。

## 运行情况
- 当前轮次: 第 1 / 1 轮
- 终止原因: 达到最大重试次数（已修复：现增加终检）

## 检查结果汇总
- PASS: 6
- FAIL: 2 ← **Tushare 2 项均为误报，文件实际存在**
- WARN: 0
- SKIP: 16

## 发现的问题

### FAIL 项
- **Tushare 源文件完整性 (9个核心文件)** ~~缺少 9 个核心文件~~ ← 误报，文件实际存在
- **财务数据完整性 (每只股票报告覆盖)** ~~缺少 4 个财务文件~~ ← 误报，同上

### WARN 项

## 尝试过的修复
- [2026-05-03T19:11:34.383252] [download] 尝试重新下载（文件已存在，可能被覆盖或跳过）
- [2026-05-03T19:11:38.116917] [update] 尝试更新财务数据（同上）

## 建议

~~1. 检查 Tushare Token 是否有效: `echo $TUSHARE_TOKEN`~~
~~2. 检查网络连接和 Tushare API 状态~~
~~3. 查看上方 FAIL 项的详细信息，手动修复~~
~~4. 修复后重新运行: `python scripts/data_quality_guard.py`~~

以上建议基于误报，无需操作。数据完整正常。

---

